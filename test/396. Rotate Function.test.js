import { maxRotateFunction } from '../src/396. Rotate Function'

describe('maxRotateFunction', () => {
  test('it calculates the maximum value from rotate functions', () => {
    expect(maxRotateFunction([4, 3, 2, 6])).toBe(26)
  })
})
