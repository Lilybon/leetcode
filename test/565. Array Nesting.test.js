import { arrayNesting } from '../src/565. Array Nesting'

describe('arrayNesting', () => {
  test('it returns the longest length of set S', () => {
    expect(arrayNesting([5, 4, 0, 3, 1, 6, 2])).toBe(4)
  })
})
