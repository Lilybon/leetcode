import { lengthOfLastWord } from '../src/58. Length of Last Word'

describe('lengthOfLastWord', () => {
  test('it returns the length of last word', () => {
    expect(lengthOfLastWord('Hello World')).toBe(5)
  })
})
